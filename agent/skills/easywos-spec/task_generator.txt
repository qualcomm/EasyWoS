/opsx:propose Run the easywos-spec pipeline on <path-to-easywos-scan-output.yaml>. The repo path is  <repo_path>. The OpenSpec change describes the orchestration workflow, not the porting itself. proposal.md: what EasyWoS found, why we run the pipeline, what it produces. design.md: decisions about script screening, LLM refinement, dispatcher routing (NOT ARM64 instruction choices). specs/: requirements for orchestration behavior (how items flow through matching, how dispatcher is invoked, how confidence/freeform/multi-spec cases are handled). tasks.md: pipeline execution steps complying with /dispatcher-skill commands that execute the leaf skills, unit test flow and other steps that easywos-spec points out. Do NOT include ARM64 porting details (register mappings, STP/LDP, NEON instructions, etc.) in any artifact — those are the leaf skills' responsibility. The orchestration layer's job is to produce the correct matched YAML and invoke the dispatcher with the right arguments. tasks.md should contain a "Dispatcher Execution" section that invokes /dispatcher-skill to route each porting item to the leaf skill tree, generate ARM64 output. Besides, tasks.md should also contain any remaining sections' guidance mentioned in the easywos-spec.